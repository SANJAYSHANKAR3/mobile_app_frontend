import { UpperCaseAndlowerCasePipe } from './upper-case-andlower-case.pipe';

describe('UpperCaseAndlowerCasePipe', () => {
  it('create an instance', () => {
    const pipe = new UpperCaseAndlowerCasePipe();
    expect(pipe).toBeTruthy();
  });
});
